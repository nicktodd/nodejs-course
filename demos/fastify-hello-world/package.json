{
  "name": "fastify-hello-world",
  "version": "1.0.0",
  "description": "Minimal Fastify API in TypeScript",
  "main": "server.ts",
  "scripts": {
    "start": "tsc && node dist/server.js",
    "dev": "ts-node server.ts"
  },
  "dependencies": {
    "fastify": "^4.24.3"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0"
  }
}
